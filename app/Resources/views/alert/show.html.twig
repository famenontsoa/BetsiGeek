{% extends 'base.html.twig' %}

{% block body %}
    <h1>Alert</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ alert.id }}</td>
            </tr>
            <tr>
                <th>Signame</th>
                <td>{{ alert.sigName }}</td>
            </tr>
            <tr>
                <th>Ipsource</th>
                <td>{{ alert.ipSource }}</td>
            </tr>
            <tr>
                <th>Sigpriority</th>
                <td>{{ alert.sigPriority }}</td>
            </tr>
            <tr>
                <th>Timestamp</th>
                <td>{{ alert.timeStamp }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('alert_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('alert_edit', { 'id': alert.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
